{
    "builders": [
        {
            "type": "digitalocean",
            "api_token": "{{user `digitalocean_api_token`}}",
            "image": "ubuntu-18-04-x64",
            "region": "{{user `digitalocean_region`}}",
            "size": "s-1vcpu-1gb",
            "ssh_username": "root"
        }
    ]    
}